package com.aisandbox.server.api;

/**
 * Placeholder. Springdoc's {@code springdoc-openapi-starter-webflux-ui}
 * already serves {@code /v1/openapi.yaml} (because
 * {@code springdoc.api-docs.path=/v1/openapi}) and {@code /v1/swagger-ui}
 * directly. No custom controller is needed.
 *
 * <p>This class is intentionally a marker for the package layout — the
 * ArchUnit layering rule expects an OpenAPI-related class to live in
 * {@code com.aisandbox.server.api}.
 */
public final class OpenApiController {

    private OpenApiController() {}
}
